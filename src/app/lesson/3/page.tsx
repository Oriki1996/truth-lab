'use client';
import { useState } from 'react';

export default function BiasLab() {
  const [step, setStep] = useState(0);
  const [confidence, setConfidence] = useState(50);
  
  const headline = "???? ???: ????? ???? ?????? ????? ?? ?????? ??????? ?-15% ???????? ??????????.";

  return (
    <main className="min-h-screen bg-slate-50 p-8 text-right" dir="rtl">
      <div className="max-w-2xl mx-auto bg-white p-8 rounded-3xl shadow-xl border border-slate-100">
        <h1 className="text-3xl font-black text-slate-900 mb-6">????? ????? 3: ????? ?????</h1>
        
        {step === 0 ? (
          <div>
            <p className="text-lg text-slate-700 mb-6 font-bold italic underline">"{headline}"</p>
            <p className="mb-4">??? ??? ???? ??????? ??? ??????? (0-100%)</p>
            <input type="range" className="w-full h-2 bg-blue-100 rounded-lg appearance-none cursor-pointer" 
                   onChange={(e) => setConfidence(parseInt(e.target.value))} />
            <div className="text-center mt-4 text-2xl font-bold text-blue-600">{confidence}%</div>
            <button onClick={() => setStep(1)} className="mt-8 w-full py-4 bg-slate-900 text-white rounded-2xl font-bold">???? ?? ????</button>
          </div>
        ) : (
          <div className="animate-in fade-in duration-700">
            <h2 className="text-2xl font-bold text-amber-600 mb-4">??????: ????? ?????? ????!</h2>
            <p className="text-slate-600 leading-relaxed">
              ??? ????? ?? Lyons, ??? ?????? ?????? ?? ?????? ???? ????? ???? ???? ?-22% ???? ???? ????????.
              ???? ?-{confidence}%, ?? ????? ?? ??? ?????? {confidence > 70 ? '????? ????' : '?????'}.
            </p>
            <button onClick={() => setStep(0)} className="mt-8 text-blue-600 font-bold">??? ????? ????</button>
          </div>
        )}
      </div>
    </main>
  );
}
